{
  "name": "ssh-tunnel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf ./dist && tsc && NODE_ENV=production node --enable-source-maps ./dist/api",
    "dev": "rm -rf ./dist && tsc && NODE_ENV=dev node -r source-map-support/register ./dist/api | bunyan -o short",
    "build-ts": "rm -rf ./dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Taylor Swanson",
  "license": "MIT"
}
